package de.jan.machinelearning.autonomouscar;

/**
 * Created by DevTastisch on 29.01.2019
 */
public class SelectMapWindow {

   /*
    private File selectedFiel = null;

    public SelectMapWindow() {
        try {
            UIManager.setLookAndFeel(UIManager.getSystemLookAndFeelClassName());
        } catch (final ClassNotFoundException | InstantiationException | IllegalAccessException | UnsupportedLookAndFeelException e) {
            e.printStackTrace();
        }

        JFileChooser fileChooser = new JFileChooser(new File("./").getAbsolutePath());
        fileChooser.setDialogTitle("Select Map:");
        fileChooser.setMultiSelectionEnabled(false);
        fileChooser.setFileSelectionMode(JFileChooser.FILES_ONLY);
        fileChooser.setFileFilter(new FileNameExtensionFilter("*.jpg", "jpg"));
        int returnVal = fileChooser.showOpenDialog(null);
        if (returnVal == JFileChooser.APPROVE_OPTION) {
            this.selectedFiel = fileChooser.getSelectedFile();
        }
    }*/
}
